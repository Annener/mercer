[project]
name = "shared_contracts"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.5,<3.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
