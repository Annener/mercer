# TD-04 — Устаревшая функция format_prompt

**Приоритет:** 🟡 Серьёзный  
**Файл:** `rag-backend/app/services/prompt_pack.py`, функция `format_prompt`

## Проблема

Функция помечена как `DEPRECATED` с комментарием:
> «будет удалён после Этапа 8 (миграции данных)».

Если Этап 8 завершён — функция является мёртвым кодом. Если нет — нужно
зафиксировать статус.

## Анализ перед исправлением

- [ ] Поиск по проекту: `from app.services.prompt_pack import format_prompt`
  и `format_prompt(` — есть ли реальные вызовы?
- [ ] Определить статус «Этапа 8» — завершён ли рефакторинг пайплайнов?
- [ ] Проверить тесты: есть ли тесты специфично для `format_prompt`?
- [ ] Если вызовы есть — оценить объём работы по миграции на `resolve_step_vars`

## Ожидаемое исправление

**Сценарий A (Этап 8 завершён, вызовов нет):**  
Удалить функцию `format_prompt` и вспомогательную `_stringify` (если она
больше нигде не используется).

**Сценарий B (Этап 8 не завершён или вызовы есть):**  
Создать задачу/milestone, убрать размытый комментарий, заменить на конкретный
TO DO с номером задачи.

## Риски

Низкие (удаление) или нет изменений (документирование статуса).
