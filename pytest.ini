[pytest]
pythonpath =
    .
    rag-backend
asyncio_mode = auto
testpaths = tests/unit
markers =
    integration: tests that require running docker compose services or external processes
    docker: docker compose smoke tests