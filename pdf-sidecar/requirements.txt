# pdf-sidecar dependencies
# Требует Python 3.11–3.13 (unstructured-inference несовместим с 3.14+)
# Установка: python3.13 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Web-сервер
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Основной парсер
unstructured[pdf]>=0.14.0

# Явные пины для стабильности
pdfminer.six>=20221105
pdf2image>=1.17.0
pillow>=10.0.0
pytesseract>=0.3.10

# ONNX Runtime — стандартный пакет.
# На macOS arm64 НЕ включает CoreMLExecutionProvider (нужна кастомная сборка).
# YOLO layout detection работает на CPU через стандартный onnxruntime.
# Table Transformer ускоряется через PyTorch MPS (Apple Silicon GPU) — см. parser.py.
# Примечание: onnxruntime-silicon (CoreML) не имеет wheel для Python 3.12/3.13.
onnxruntime>=1.17.0

# Для корректного парсинга HTML-таблиц из unstructured (lxml ускоряет HTMLParser)
lxml>=5.0.0

# Утилиты
pyyaml>=6.0
