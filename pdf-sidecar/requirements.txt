# pdf-sidecar dependencies
# Требует Python 3.11–3.13 (unstructured-inference несовместим с 3.14+)
# Установка: python3.13 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Web-сервер
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Основной парсер
unstructured[pdf]>=0.14.0

# Явные пины для стабильности
pdfminer.six>=20221105
pdf2image>=1.17.0
pillow>=10.0.0
pytesseract>=0.3.10

# ONNX Runtime — стандартный пакет.
# На macOS arm64 НЕ включает CoreMLExecutionProvider (нужна кастомная сборка).
# YOLO layout detection работает на CPU через стандартный onnxruntime.
# Table Transformer ускоряется через PyTorch MPS (Apple Silicon GPU) — см. parser.py.
# Примечание: onnxruntime-silicon (CoreML) не имеет wheel для Python 3.12/3.13.
onnxruntime>=1.17.0

# Для корректного парсинга HTML-таблиц из unstructured (lxml ускоряет HTMLParser)
lxml>=5.0.0

# Утилиты
pyyaml>=6.0

# Ghostscript Python-биндинги (системный gs ставится через brew — см. install.sh)
# Нужен для _normalize_with_ghostscript в parser.py:
# если PDFium не может открыть битый PDF — нормализуем через gs и повторяем hi_res.
ghostscript>=0.7

# Reranker — CrossEncoder для /rerank endpoint
# torch: MPS-ускорение на Apple Silicon (автоопределение в reranker.py)
# sentence-transformers: обёртка над CrossEncoder (bge-reranker-v2-m3 и др.)
torch>=2.2.0
sentence-transformers>=3.0.0

# Drift detector (Phase 2a context-engine) — локальная QVikhr-3-1.7B-Instruct-noreasoning
# (Q4_K_M, GGUF, ~1.1 GB) через llama.cpp. На macOS M-серии используется Metal GPU
# (DRIFT_FORCE_CPU=0 по умолчанию). Бинарь llama-cpp-python ставит wheel-ы под
# основные платформы (macOS arm64/x86_64, linux x86_64). На Linux без wheel-а
# требуется сборка из исходников (нужен gcc + cmake):
#   CMAKE_ARGS="-DGGML_METAL=OFF" pip install --force-reinstall --no-cache-dir llama-cpp-python
llama-cpp-python>=0.3.0

# Для скачивания .gguf из HuggingFace Hub в install.sh (шаг [9/9] — drift-модель).
# Пакет уже тянется транзитивно через sentence-transformers, но добавляем явный пин
# для стабильности будущих обновлений.
huggingface_hub>=0.24.0
